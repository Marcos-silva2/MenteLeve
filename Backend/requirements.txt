# Versões com wheels disponíveis para Python 3.11–3.14.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
sqlalchemy>=2.0.36,<2.1
pydantic[email]>=2.11,<3.0
psycopg[binary]>=3.1,<4.0  # driver Postgres (Supabase) — usar DATABASE_URL com esquema postgresql+psycopg://
httpx>=0.27,<1.0  # cliente assíncrono para Gemini/Groq (app/ai.py) — não bloqueia o threadpool
pywebpush>=2,<3  # notificações push (app/push.py) — protocolo Web Push assinado com VAPID
bcrypt>=4.0,<5.0
pyjwt>=2.9,<3.0
cryptography>=42,<47  # AES-256-GCM do conteúdo em repouso (app/crypto.py) — wheel abi3, sem compilação

# Testes (rodar com `pytest` em Backend/) — não são usados em produção.
pytest>=8,<10
